<% if current_user %>
  <%= form_for [@review.neighborhood, @review] do |f| %>
     <p>
      <%= f.label :title  %><br>
      <%= f.text_field :title %>
    </p>

    <p>
      <%= f.label :body  %><br>
      <%= f.text_field :body %>
    </p>

    <%= f.submit %>
  <% end %>
<% end %>
